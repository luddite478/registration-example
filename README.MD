Node.js registration example

To run this app specify mongodb url and secret for session in config.js
This app also requires redis

Run using **npm start** or to run with docker-compose use **docker-compose up**
